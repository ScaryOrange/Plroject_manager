version: '3.8'

services:
  project-manager:
    build: .
    container_name: project-manager-app
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge